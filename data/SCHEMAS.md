# Data Schemas

All CSV and JSON data contracts are defined in [`schemas.yaml`](schemas.yaml).

That file is the single source of truth — read by both `export_paper_data.py` (KD-GAT) and `pull_data.py` (this repo).
