[project]
name = "kd-gat-paper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "bibtexparser",
    "pandas",
    "polars",
    "pyyaml",
    "scipy",
    "tabulate",
]

[project.optional-dependencies]
test = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
