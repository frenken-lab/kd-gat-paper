Missing figures from old dashboard:

- bubble charts (F1, acc, AUC across models and datasets)
- Pareto Frontier: (metric to param count)
- counting stats
- Training stats
- Resource stats (will be new contours more details)
- GAT attention heatmap layers vs heads hue is attention
- fusion policy
- KD transfer (F1 vs F1, metric vs metric) + CKA
- Umap per model per training type with contour density
- Graph samples force-layout D3
- Dataset statistics:
  - attack distribution raw
  - attack stats across graph features (density, edges, nodes, payload etc)
  - adjacency matrix by attack type
  - density, clustering coefficient, etc
  - summary table
